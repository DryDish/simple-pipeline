{
	"name": "hook-server",
	"version": "1.0.0",
	"description": "Small server to listen for GitHub hooks",
	"main": "src/app.js",
	"scripts": {
		"start": "tsc -b tsconfig.json && node ./build/app.js",
		"build": "tsc -b tsconfig.json",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Alex M",
	"dependencies": {
		"dotenv": "^16.0.1",
		"express": "^4.17.3",
		"shelljs": "^0.8.5"
	},
	"devDependencies": {
		"@types/express": "^4.17.13",
		"@types/shelljs": "^0.8.11",
		"typescript": "^4.7.4"
	}
}
